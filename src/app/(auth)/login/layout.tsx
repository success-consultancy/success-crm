import { Metadata } from 'next';
import LoginPage from './page';

export const metadata: Metadata = {
  title: 'Login | Success Education',
  description: 'Sign in to your Success Education account',
};

export default LoginPage;
